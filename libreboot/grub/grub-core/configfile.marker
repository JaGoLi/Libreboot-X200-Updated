INPUT_TERMINAL_LIST_MARKER(const char *name __attribute__ ((unused)), grub_term_input_t term)
OUTPUT_TERMINAL_LIST_MARKER(const char *name __attribute__ ((unused)), grub_term_output_t term)
COMMAND_LIST_MARKER(const char *name, grub_command_func_t func, const char *summary, const char *description)
P1COMMAND_LIST_MARKER(const char *name, grub_command_func_t func, const char *summary, const char *description)
    COMMAND_LIST_MARKER("configfile", grub_cmd_source, "FILE", "Load another config file.")
    COMMAND_LIST_MARKER("source", grub_cmd_source, "FILE", "Load another config file without changing context.")
    COMMAND_LIST_MARKER("extract_entries_source", grub_cmd_source, "FILE", "Load another config file without changing context but take only menu entries.")
    COMMAND_LIST_MARKER("extract_entries_configfile", grub_cmd_source, "FILE", "Load another config file but take only menu entries.")
    COMMAND_LIST_MARKER(".", grub_cmd_source, "FILE", "Load another config file without changing context.")
